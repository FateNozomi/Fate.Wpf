<UserControl x:Class="Fate.Wpf.Controls.LabelTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fate.Wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="23" d:DesignWidth="200"
             x:Name="Self">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding LabelWidth, ElementName=Self}"/>
            <ColumnDefinition Width="{Binding ValueWidth, ElementName=Self}"/>
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Column="0"
            Text="{Binding Label, ElementName=Self}"
            HorizontalAlignment="{Binding LabelAlignment, ElementName=Self}"
            VerticalAlignment="Center"
            TextWrapping="Wrap"/>
        <TextBox
            Grid.Column="1"
            Text="{Binding Value, ElementName=Self, UpdateSourceTrigger=PropertyChanged}"
            IsReadOnly="{Binding ValueReadOnly, ElementName=Self}"
            TextAlignment="{Binding ValueAlignment, ElementName=Self}"
            Margin="{Binding ValueMargin, ElementName=Self}"
            VerticalContentAlignment="Center"/>
    </Grid>
</UserControl>
